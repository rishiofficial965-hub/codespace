kubectl create secret generic auth-secret --from-env-file=.env

kubectl create secret generic redis-secret --from-literal=redis=your_redis_url

env:
  - name: REDIS_URL
    valueFrom:
      secretKeyRef:
        name: redis-secret
        key: redis  